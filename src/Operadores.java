
public class Operadores {
	
	
	public static void main(String[] args) {
		
	
	/**
	 * Os operadores são simbolos especiais que são chave dentro da plataforma
	 * ele determinar comparações e algumas outras ações a serem feitas que veremos
	 * mais abaixo em cada tópico
	 */
	
		//CLASSIFICACAO DOS OPERADORES
		/**
		 * iremos abaixo comentar e descrever cada tipo de operador e como ele
		 * funciona dentro do codigo
		 */
	
	//OPERADORES DE ATRIBUICAO
	/**
	 * O operador de atribuicao e utilizado para definir o valor inicial ou sobrescrever
	 * o valor de uma variavel em java, primeiro definimos um tipo a essa variavel e apos isso
	 *iremos colocar o sinal de atribuicao e algum conteudo dentro como no exemplo abaixo 
	 */
	
		/**
		 * adentre a classe abaixo para pode ver exemplos e como utiliza-la
		 */
		OperadoresDeAtribuicao atribuicao = new OperadoresDeAtribuicao();
		
		
	//OPERADORES ARITMÉTICOS
	/**
	 * O operador aritmético é utilizado para tratar e realizar operacoes matematicas entre
	 * valores numericos, podendo se tornar ou não uma expressao mais complexa
	 */
		
		/**
		 * adentre a classe abaixo para pode ver exemplos e como utiliza-la
		 */
		
		OperadoresAritmericos aritmeticos = new OperadoresAritmericos();
	
	//OPERADORES UNÁRIOS
		/**
		 * 
		 * Esses operadores, são aplicados juntamente com um outro operador aritmético. Eles realizam alguns trabalhos básicos como incrementar, decrementar, inverter valores numéricos e booleanos.
		 *	(+) Operador unário de valor positivo – números são positivos sem esse operador explicitamente;
		 *	(-) Operador unário de valor negativo – nega um número ou expressão aritmética;
		 *	(++) Operador unário de incremento de valor – incrementa o valor em 1 unidade;
		 *	(--) Operador unário de decremento de valor – decrementa o valor em 1 unidade;
		 *	(!) Operador unário lógico de negação – nega o valor de uma expressão booleana.
		 *
		 */
	
				/**
			 	* adentre a classe abaixo para pode ver exemplos e como utiliza-la
			 	*/
		OperadoresUnarios unarios = new OperadoresUnarios(); 
		
		
		
		//OPERADORES TERNARIOS
			/**
			 * 
			 *  O operador de condição ternária é uma forma resumida para definir uma condição e escolher por um dentre dois valores. Você deve pensar numa condição ternária, como se fosse uma condição IF normal, porém, de uma forma em que toda a sua estrutura estará escrita numa única linha.
			 *  O operador ternário é representado pelos símbolos "?" e ":" utilizados na seguinte estrutura de sintaxe:
			 *	<Expressão Condicional>`` ?``<Caso condição seja true>``: ``<Caso condição seja false>
			 * 
			 */
		
					/**
					 * adentre a classe abaixo para pode ver exemplos e como utiliza-la
					*/
		
		OperadoresTernarios ternarios = new OperadoresTernarios();
		
		
		//OPERADORES RELACIONAIS
			/**
			 * 
			 *  Os operadores relacionais, avaliam a relação entre duas variáveis ou expressões. Neste caso, mais precisamente, definem se o operando à esquerda é igual, diferente, menor, menor ou igual, maior, maior ou igual ao da direita, retornando um valor booleano como resultado.
			 *	== Quando desejamos verificar se uma variável é IGUAL A outra.
			 *	!= Quando desejamos verificar se uma variável é DIFERENTE da outra.
			 *	> Quando desejamos verificar se uma variável é MAIOR QUE a outra.
			 *	>= Quando desejamos verificar se uma variável é MAIOR OU IGUAL a outra.
			 *	< Quando desejamos verificar se uma variável é MENOR QUE outra.
			 *	<= Quando desejamos verificar se uma variável é MENOR OU IGUAL a outra.
			 * 
			 */
		
			OperadoresRelacionais relacionais = new OperadoresRelacionais();
			
			
			//COMPARACOES AVANCADAS
			
			/**
			 * Quando se refere a comparação de conteúdos na linguagem,
			 *  devemos ter um certo domínio, de 
			 * como o Java trata o armazenamento destes valores na memória.
			 */
			
			/**
			 * adentre a classe abaixo para pode ver exemplos e como utiliza-la
			*/
			
				ComparacaoEntreObjetos comparacao = new ComparacaoEntreObjetos();
			
			
				
				//OPERADORES LOGICOS
					/**
					 * 
					 * Os operadores lógicos, representam o recurso que nos permite criar expressões lógicas maiores, a partir da junção de duas ou mais expressões.
					 *  && Operador Lógico "E"
					 *  || Operador Lógico "OU"
					 *  
					 */
				
						/**
				 		* adentre a classe abaixo para pode ver exemplos e
				 		*  como utiliza-la
				 		*/
				
				OperadoresLogicos logicos = new OperadoresLogicos();
				
				
				
				
				
				
				
	}
}
